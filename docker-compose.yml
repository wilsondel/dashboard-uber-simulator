services:
  dashboard:
    build: .
    container_name: ridesim-dashboard
    ports:
      - "8501:8501"
    environment:
      POSTGRES_HOST: 18.204.73.160
      POSTGRES_DB: ridesim
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    restart: unless-stopped
